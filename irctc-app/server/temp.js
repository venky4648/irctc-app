const { pool } = require('./shared/utils/db.js'); require('dotenv').config(); async function init() { try { await pool.query(CREATE TABLE IF NOT EXISTS trains (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), train_number VARCHAR(50) UNIQUE NOT NULL, name VARCHAR(255) NOT NULL, departure_time TIME, arrival_time TIME, created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP); CREATE TABLE IF NOT EXISTS train_routes (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), train_id UUID REFERENCES trains(id) ON DELETE CASCADE, halt_order INTEGER NOT NULL, station_name VARCHAR(255) NOT NULL, arrival_time TIME, departure_time TIME);); console.log('Tables created'); process.exit(0); } catch (err) { console.error(err); process.exit(1); } } init();
